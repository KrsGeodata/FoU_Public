<?xml version="1.0" encoding="utf-8"?>
<Page
    x:Class="LocalLLMApp.Views.ProfilePage"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:local="using:LocalLLMApp.Views"
    xmlns:userControls="using:LocalLLMApp.UserControls"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    mc:Ignorable="d">

    <Page.Resources>
        <ResourceDictionary Source="/UserControls/ResourceDictionary.xaml"/>
    </Page.Resources>

    <Grid>
        <Grid Padding="16" RowSpacing="8">
            <Grid.RowDefinitions>
                <RowDefinition Height="*"/>
                <RowDefinition Height="Auto"/>
                <RowDefinition Height="*"/>
            </Grid.RowDefinitions>
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="*"/>
                <ColumnDefinition Width="Auto"/>
                <ColumnDefinition Width="*"/>
            </Grid.ColumnDefinitions>

            <StackPanel Orientation="Horizontal" Spacing="16" Grid.Row="1" 
                Grid.Column="1" HorizontalAlignment="Center">
                <PersonPicture Initials="{x:Bind ViewModel.Initials, Mode=OneWay}"/>
                <StackPanel 
                    Orientation="Vertical" Spacing="16" Padding="20, 0, 20, 0" MinWidth="350" MaxWidth="350">

                    <TextBlock Text="Din profil" 
                               HorizontalAlignment="Left" 
                               Style="{StaticResource TitleLargeTextBlockStyle}"/>

                    <TextBlock Text="Navn:" Style="{StaticResource SubtitleTextBlockStyle}" />
                    <TextBlock Text="{x:Bind ViewModel.Name, Mode=OneWay}"/>
                    <TextBlock Text="E-post:" Style="{StaticResource SubtitleTextBlockStyle}" />
                    <TextBlock Text="{x:Bind ViewModel.Email, Mode=OneWay}"/>

                </StackPanel>
            </StackPanel>
        </Grid>
    </Grid>
</Page>
