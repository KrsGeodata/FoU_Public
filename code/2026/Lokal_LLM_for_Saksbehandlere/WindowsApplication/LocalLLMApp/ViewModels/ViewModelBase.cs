using CommunityToolkit.Mvvm.ComponentModel;

// <Purpose>
// This ViewModel is a generic template for how a ViewModel should look.
// </Purpose>
// Consider either fleshing it out more or maybe removing it if it's not needed,
// as it doesn't add much value in its current state.

namespace LocalLLMApp.ViewModels
{
    public abstract class ViewModelBase : ObservableObject
    {
    }
}
